# frozen_string_literal: true

RSpec.describe Odin::Ruby::Tic::Tac::Toe do
  it "has a version number" do
    expect(Odin::Ruby::Tic::Tac::Toe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
